server:
  port: 7021
spring:
  datasource:
    url: jdbc:mysql://192.168.0.122:3306/yard_ums
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml # classpath* 将依赖包的 xml 也扫描到
  global-config:
    db-config:
      id-type: auto
